/bin/
/dist/
/PROGRESS.md
*.db
*.db-shm
*.db-wal
.env*
__pycache__/
RESULT.json
# Test-run artifacts (governed-run scratch state left behind by TestX helpers
# that build a real in-process Run; never tracked, harmless, ignored so a
# post-test working tree stays clean).
output/
.governator/
.governator-canary
# Sol9 P2-2: contamination the audit found inside a manually-tarred bundle --
# stray scratch/debug files and the context-graph runtime db, none of which
# scripts/audit_bundle.sh's git-archive-based source/ tier can ever include
# once they're also excluded from tracking here.
.tmp_debug_*
.codegraph/
